spring:
  application:
    name: vacancy-service
  profiles:
    active: ${PROFILE:dev}
  jackson:
    property-naming-strategy: SNAKE_CASE
  config:
    activate:
      on-profile: dev
    import: optional:configserver:http://localhost:${CONFIG_SERVER_PORT:9991}

#eureka:
#  client:
#    enabled: true
#    register-with-eureka: true
#    fetch-registry: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka
